{
  "version": 2,
  "buildCommand": "cd frontend && npm run build && cd ../api && npm install && cd ../backend && npm install --production",
  "installCommand": "cd frontend && npm install && cd ../api && npm install && cd ../backend && npm install --production",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ],
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "{backend/**,backend/node_modules/**}"
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
